export const metadata = { title: 'vveritas* — built for Ron Blake.' };
export default function PitchLayout({ children }) {
  return <div style={{ background: '#000', minHeight: '100vh' }}>{children}</div>;
}
