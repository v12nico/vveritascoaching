export const metadata = { title: 'vveritas* — master yourself.' };

export default function WebinarLayout({ children }) {
  return <div style={{ background: '#000', minHeight: '100vh' }}>{children}</div>;
}
